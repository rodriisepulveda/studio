{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist",
          "installCommand": "npm install --production=false && chmod +x node_modules/.bin/vite"
        }
      }
    ]
  }